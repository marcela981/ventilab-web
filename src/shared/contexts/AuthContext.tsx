/**
 * @deprecated Import from '@/contexts/AuthContext' for now
 * Bridge file - will become the canonical location in future phases
 */
export { AuthProvider, useAuth, RequireAuth, RequireRole } from '@/contexts/AuthContext';
