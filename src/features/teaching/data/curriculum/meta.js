/**
 * Curriculum Metadata
 * Configuration for curriculum module strategy
 * 
 * Strategy options:
 * - "data-driven": Only show real modules (no placeholders)
 * - "roadmap": Show real modules + placeholders (complete roadmap)
 */

export const curriculumMeta = {
  declaredTotal: 11,
  strategy: "data-driven"
};

export default curriculumMeta;

